# Lasso Regression

## How to run
* cd $ROOT/src/
* run `python`
```
import lasso as l
# decide how many k fold to be used
l.predictPerStore(k)
```
* result will be stored in $ROOT/data/lasso_result.csv

## Dataset
* The preprocessing for the dataset is done on python shell directly, thus the code is not available
