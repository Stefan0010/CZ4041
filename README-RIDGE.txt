# Ridge Regression

## How to run
* cd $ROOT/src/
* run `python`
```
import ridge as r
# decide how many k fold to be used
r.predictPerStore(k)
```
* result will be stored in $ROOT/data/ridge_result.csv

## Dataset
* The preprocessing for the dataset is done on python shell directly, thus the code is not available
